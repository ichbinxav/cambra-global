{
  "name": "RateLimitCounter",
  "type": "object",
  "properties": {
    "principal_id": {
      "type": "string",
      "description": "API key/OAuth subject or versioned HMAC network fingerprint. Raw network addresses and proxy headers are forbidden."
    },
    "principal_type": {
      "type": "string",
      "enum": [
        "api_key",
        "oauth_token",
        "network_hmac"
      ],
      "default": "api_key"
    },
    "window_start": {
      "type": "string",
      "format": "date-time",
      "description": "Start of the current 60-second window"
    },
    "count": {
      "type": "number",
      "default": 0
    },
    "limit_per_minute": {
      "type": "number",
      "default": 120
    },
    "window_seconds": {
      "type": "number",
      "minimum": 1,
      "description": "Actual bucket duration. limit_per_minute is retained as a legacy field name and stores the configured bucket limit."
    }
  },
  "required": [
    "principal_id",
    "principal_type",
    "window_start",
    "window_seconds"
  ],
  "rls": {
    "read": {
      "user_condition": {
        "role": "__service_role_only__"
      }
    },
    "write": {
      "user_condition": {
        "role": "__service_role_only__"
      }
    }
  }
}
