{
  "name": "WebhookDeadLetter",
  "type": "object",
  "properties": {
    "webhook_id": {
      "type": "string"
    },
    "webhook_name": {
      "type": "string"
    },
    "event_type": {
      "type": "string"
    },
    "target_url": {
      "type": "string"
    },
    "payload": {
      "type": "object"
    },
    "last_response_code": {
      "type": "number"
    },
    "last_response_body": {
      "type": "string"
    },
    "last_error_message": {
      "type": "string"
    },
    "total_attempts": {
      "type": "number",
      "default": 0
    },
    "next_retry_at": {
      "type": "string",
      "format": "date-time"
    },
    "locked_at": {
      "type": "string",
      "format": "date-time",
      "description": "SECURITY-2 (2026-07-24) \u2014 atomic-ish claim set by processWebhookDeadLetters BEFORE re-delivery. A row locked less than 10 minutes ago is skipped by concurrent runs (prevents double sends). Cleared on resolution/reschedule. Additive \u2014 historical rows have it absent."
    },
    "status": {
      "type": "string",
      "enum": [
        "pending_retry",
        "exhausted",
        "resolved",
        "abandoned"
      ],
      "default": "pending_retry"
    },
    "first_failed_at": {
      "type": "string",
      "format": "date-time"
    },
    "resolved_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "webhook_id",
    "event_type",
    "target_url",
    "status"
  ],
  "rls": {
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "write": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}