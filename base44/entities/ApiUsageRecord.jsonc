{
  "name": "ApiUsageRecord",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string"
    },
    "period_month": {
      "type": "string",
      "description": "YYYY-MM bucket (e.g. 2026-06)"
    },
    "request_count": {
      "type": "number",
      "default": 0
    },
    "included_quota": {
      "type": "number"
    },
    "overage_count": {
      "type": "number",
      "default": 0
    },
    "overage_amount_eur": {
      "type": "number",
      "default": 0
    },
    "billed": {
      "type": "boolean",
      "default": false
    },
    "billed_at": {
      "type": "string",
      "format": "date-time"
    },
    "billing_run_id": {
      "type": "string",
      "description": "SECURITY-2 (2026-07-24) \u2014 claim marker set by billApiUsage BEFORE invoicing. A record claimed by one run is skipped by concurrent/duplicate runs. Released (null) if invoice creation fails so the next run retries. Additive \u2014 historical rows have it absent."
    },
    "stripe_invoice_id": {
      "type": "string"
    },
    "last_updated_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "organization_id",
    "period_month"
  ],
  "rls": {
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "write": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}