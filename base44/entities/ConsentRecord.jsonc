{
  "name": "ConsentRecord",
  "type": "object",
  "properties": {
    "brand_id": {
      "type": "string",
      "description": "Brand this consent belongs to."
    },
    "provider": {
      "type": "string",
      "description": "External provider (e.g. 'stripe')."
    },
    "scope": {
      "type": "string",
      "description": "Human-readable description of what access was granted (e.g. 'read_only')."
    },
    "purpose": {
      "type": "string",
      "enum": [
        "data_connection",
        "provider_introduction",
        "provider_negotiation",
        "marketing",
        "other"
      ],
      "description": "Specific purpose. Provider introduction/negotiation consent must be counterparty-specific."
    },
    "counterparty_provider_id": {
      "type": "string",
      "description": "Provider ID covered by a provider introduction or negotiation consent."
    },
    "consent_text_version": {
      "type": "string",
      "description": "Immutable version identifier of the text accepted by the merchant."
    },
    "evidence_reference": {
      "type": "string",
      "description": "Reference to the signed mandate, clickwrap receipt, or other consent evidence."
    },
    "expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "granted_at": {
      "type": "string",
      "format": "date-time"
    },
    "revoked_at": {
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "revoked",
        "expired"
      ],
      "default": "active"
    },
    "access_token_ref": {
      "type": "string",
      "description": "Opaque reference ID for the access token \u2014 NEVER the raw token."
    },
    "metadata": {
      "type": "object",
      "description": "Provider-specific connection metadata. NEVER contains raw credentials."
    }
  },
  "required": [
    "brand_id",
    "provider"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    },
    "write": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    }
  }
}