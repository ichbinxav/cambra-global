{
  "name": "PaymentsAnalysisSession",
  "type": "object",
  "properties": {
    "anon_session_id": {
      "type": "string",
      "description": "UUID v4 generated server-side by submitPaymentsAnalysis (crypto.randomUUID()). Primary key by which the Chunk 5 teaser retrieves this result. No uniqueness constraint at the entity level (Base44 does not support them); collision probability at UUID v4 scale is negligible for our volume. Never exposed to authenticated users through general RLS \u2014 read path is service-role-only via getAnonResultTeaser."
    },
    "contact_email": {
      "type": "string",
      "description": "UX-1-FIX T2 (2026-07-31) \u2014 contact email for report delivery, captured by the mandatory email gate (UX-1 T1). PII with its OWN retention/anonymization policy, deliberately separated from input_snapshot so it can be scrubbed independently of the engine inputs (DATA-1 anticipates a different treatment for unclaimed anonymous sessions). Internal/lead use only: NEVER returned by getPaymentsGapTeaser's allowlist, never used for marketing without a separate explicit opt-in. Rows created before 2026-07-31 carry the email inside input_snapshot.email instead (historical; the 90-day TTL purge retires them naturally)."
    },
    "locale": {
      "type": "string",
      "enum": [
        "en",
        "fr",
        "es"
      ],
      "default": "en",
      "description": "EMAIL-1 (2026-07-31) \u2014 language in which any email tied to this session (report delivery, follow-up) must be sent. Captured client-side from the ACTIVE UI language at submit time (the same value that governs the cambra_lang key), never inferred from Accept-Language in the backend: a manual pick in the switcher is a deliberate choice and wins (UX-1 T0 doctrine). Absent/unrecognized \u2192 'en' via normalizeLocale (base44/shared/emailLocale.ts). Sits at the top level NEXT TO contact_email, not inside input_snapshot, for the same reason: it is delivery metadata, never an engine input, so it can never drift a calculation. NEVER returned by getPaymentsGapTeaser \u2014 that endpoint's hard allowlist copies fields by name and this one is deliberately not on it."
    },
    "referred_by_code": {
      "type": "string",
      "description": "GROWTH-1 T2 (2026-08-01) \u2014 opaque referral code (?ref=<code>) that brought this visitor to the Analyzer. Persisted ONLY when the code matches an existing ReferralLink row (validated server-side in submitPaymentsAnalysis; unknown/malformed codes drop silently so a mistyped link never breaks a submit). Attribution metadata only \u2014 never an engine input, and NEVER returned by getPaymentsGapTeaser or the anonymous submit response (both use hard allowlists that copy fields by name; this one is deliberately not on them). Joins back to ReferralLink.code to measure which invites convert."
    },
    "time_to_result_ms": {
      "type": "number",
      "description": "GROWTH-1 T3 (2026-08-01) \u2014 client-measured milliseconds from the Analyzer page mount to the submit request. Bounded server-side to 0..1,800,000 (30 min); out-of-range values drop silently. No PII, no extra identifiers. NOTE ON SEMANTICS: this captures the FORM-FILLING portion of time-to-value; the render of the result adds submit latency + redirect + teaser read on top (those server latencies are measured separately \u2014 see Decision_Log_GROWTH1). Never returned by the teaser allowlist."
    },
    "input_snapshot": {
      "type": "object",
      "description": "The post-validation, sanitized inputs that produced this session's engine_result. Fields: monthly_gmv_eur (number), avg_ticket_eur (number), region (EU|UK|US|RoW derived server-side from country), country (ISO-3166-1 alpha-2), provider_slug (from the sanctioned enum), intl_pct (0-100), card_mix_debit_pct (0-100, optional \u2014 may be omitted when the caller did not supply it), channel, plus optional lead metadata (brand_name, website, sector). Combined-mode rows additionally carry mode:'combined' + channels[]. Stored post-validation so audits show what the engine actually saw, not the raw payload. card_mix_debit_pct is persisted for future engine versions that will consume it; today's engine (v1) ignores it and its presence does not affect the computed savings. Since UX-1-FIX T2 (2026-07-31) the visitor's email is NOT part of this object \u2014 it lives in the dedicated contact_email field (rows older than that date may still carry a legacy email key here)."
    },
    "engine_result": {
      "type": "object",
      "description": "Full verbatim output of calculatePaymentsGap: engine_version, current_effective_bps, achievable_effective_bps, monthly_savings_eur {lo,point,hi}, annual_savings_eur {lo,point,hi}, cohort {key,verified,matched}, assumptions[]. Persisted as-returned so the Chunk 5 teaser can render the session without any recomputation."
    },
    "engine_version": {
      "type": "string",
      "description": "Version tag persisted from engine_result.engine_version. The session records what the engine said \u2014 never a caller-side constant. Chunk 5 may use this to filter out sessions produced by a prior engine version if the SYNC block's arithmetic ever changes."
    },
    "ip_hash": {
      "type": "string",
      "description": "Legacy field name. New rows store only the versioned, domain-separated HMAC network fingerprint (rlh:<key-version>:<digest>) produced by rateLimit.ts. It is never a raw IP or an unkeyed SHA digest."
    },
    "claim_state": {
      "type": "string",
      "enum": [
        "UNCLAIMED",
        "CLAIMED",
        "MATERIALIZING",
        "RECONCILE_REQUIRED",
        "COMPLETED"
      ],
      "default": "UNCLAIMED",
      "description": "Durable authority for anonymous-to-authenticated ownership. Once CLAIMED, ownership is never released to a different email; failures remain resumable by the canonical owner."
    },
    "claim_revision": {
      "type": "number",
      "default": 0,
      "description": "Monotonic CAS fence for every ownership/materialization transition."
    },
    "claim_token": {
      "type": "string",
      "description": "Opaque fencing token minted only when the authenticated email atomically claims this session."
    },
    "claim_owner": {
      "type": "string",
      "description": "Normalized authenticated email that permanently owns this session after the first successful CAS claim."
    },
    "claim_acquired_at": {
      "type": "string",
      "format": "date-time"
    },
    "claim_updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "claim_attempts": {
      "type": "number",
      "default": 0
    },
    "claim_error_code": {
      "type": "string",
      "description": "Retryable internal phase error. Never exposed to anonymous claim callers."
    },
    "claim_analyzer_result_id": {
      "type": "string",
      "description": "Canonical AnalyzerResult created for this claim; immutable once bound."
    },
    "claim_brand_id": {
      "type": "string",
      "description": "Brand selected or created by the canonical owner during resumable materialization."
    },
    "claim_intelligence_snapshot_id": {
      "type": "string",
      "description": "Winner-only IntelligenceSnapshot bound after tenant/brand checks."
    }
  },
  "required": [
    "anon_session_id",
    "input_snapshot",
    "engine_result",
    "engine_version"
  ],
  "rls": {
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "write": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}
