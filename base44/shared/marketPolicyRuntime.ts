import{canonicalMarket}from'./marketContext.ts';import{assertNoAiOverride,MARKET_CAPABILITIES}from'./jurisdictionPolicy.ts';
export async function evaluateMarketCapabilityRuntime(svc:any,input:any){const capability=String(input?.capability||'').toUpperCase();if(!MARKET_CAPABILITIES.includes(capability))return{allowed:false,state:'BLOCKED',reason_code:'unknown_capability',enforced:true};let brand=input?.brand||null;if(!brand&&input?.brand_id)brand=await svc.entities.Brand.get(String(input.brand_id)).catch(()=>null);let context=input?.context||null;if(!context&&brand?.market_context_id)context=await svc.entities.MerchantMarketContext.get(String(brand.market_context_id)).catch(()=>null);if(!context&&brand){const rows=await svc.entities.MerchantMarketContext.filter({brand_id:brand.id},'-last_resolved_at',1).catch(()=>[]);context=rows[0]||null}const explicit=canonicalMarket(input?.jurisdiction)?.iso2||null;const jurisdiction=explicit||canonicalMarket(context?.legal_entity_country)?.iso2||canonicalMarket(context?.home_market)?.iso2||canonicalMarket(brand?.billing_country)?.iso2||canonicalMarket(brand?.country)?.iso2||null;const rollout=String(input?.rollout||brand?.market_context_rollout||'legacy');const enforced=input?.enforce===true||rollout==='production';if(!jurisdiction){const d={allowed:!enforced,state:enforced?'BLOCKED':'REVIEW_REQUIRED',reason_code:enforced?'market_jurisdiction_unresolved':'shadow_jurisdiction_unresolved',jurisdiction:null,enforced,rollout};return d}const policies=await svc.entities.JurisdictionCapabilityPolicy.filter({jurisdiction,capability,active:true},'-effective_from',20).catch(()=>[]);let policy=policies[0]||null;const controls=await svc.entities.MarketCapabilityControl.filter({jurisdiction,capability,blocked:true},'-updated_at',20).catch(()=>[]),now=Date.now(),control=controls.find((x:any)=>(!x.effective_from||Date.parse(x.effective_from)<=now)&&(!x.effective_to||Date.parse(x.effective_to)>now))||null;const overrides=await svc.entities.MarketPolicyOverride.filter({jurisdiction,capability},'-created_at',20).catch(()=>[]),override=overrides.find((x:any)=>!x.revoked_at&&(!x.effective_from||Date.parse(x.effective_from)<=now)&&(!x.expires_at||Date.parse(x.expires_at)>now))||null;const nonOverridable=['ACCESS_BANK_ACCOUNT_DATA','INITIATE_PAYMENT','HOLD_FUNDS','ACT_AS_PSP','ACT_AS_PSP_AGENT'].includes(capability);if(override&&!nonOverridable)policy={...(policy||{}),state:override.new_state,reason_code:'explicit_human_override',policy_version:`override:${override.id}`};const raw=assertNoAiOverride({capability,policy,kill_switch:!!control,within_limit:input?.within_limit===true,ai_requested_bypass:input?.ai_requested_bypass===true,now:new Date().toISOString()});const decision=enforced?raw:{...raw,allowed:true,shadow_would_allow:raw.allowed,shadow_state:raw.state,shadow_reason_code:raw.reason_code,reason_code:'shadow_mode_not_enforced'};return{...decision,jurisdiction,enforced,rollout,policy_id:policy?.id||null,control_id:control?.id||null,override_id:override?.id||null,non_overridable:nonOverridable,context_id:context?.id||null}}
export async function auditMarketCapabilityDecision(svc:any,input:any,decision:any){const eventType=decision.bypass_attempt?'country_policy_ai_bypass_denied':decision.enforced?(decision.allowed?'country_policy_allowed':'country_policy_denied'):'country_policy_checked';await svc.entities.Event.create({brand_id:String(input?.brand_id||input?.brand?.id||'_platform'),event_type:eventType,source:String(input?.actor_type||'market_policy_runtime'),entity_type:'JurisdictionCapabilityPolicy',entity_id:decision.policy_id||'',payload_json:{jurisdiction:decision.jurisdiction,capability:String(input?.capability||'').toUpperCase(),decision,policy_id:decision.policy_id,control_id:decision.control_id,override_id:decision.override_id,context_id:decision.context_id},status:'processed',processed_at:new Date().toISOString()}).catch(()=>null);return decision}
export async function assertMarketCapabilityAllowed(svc:any,input:any){const d=await evaluateMarketCapabilityRuntime(svc,input);await auditMarketCapabilityDecision(svc,input,d);if(!d.allowed){const e:any=new Error(`market_capability_denied:${input.capability}:${d.reason_code}`);e.code='MARKET_CAPABILITY_DENIED';e.decision=d;throw e}return d}
