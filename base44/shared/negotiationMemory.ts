export function negotiationCohort(c:any){return [String(c?.provider_name||'unknown').toLowerCase(),String(c?.currency||'EUR').toUpperCase(),String(c?.current_economics_json?.pricing_model||'unknown').toLowerCase()].join('|')}
export function safeMemorySummary(m:any){if(!m||Number(m.sample_size||0)<3)return null;return {sample_size:Number(m.sample_size||0),median_rounds:Number(m.median_rounds||0),best_observed_variable_fee_bps:m.best_observed_variable_fee_bps==null?null:Number(m.best_observed_variable_fee_bps),avg_improvement_bps:m.avg_improvement_bps==null?null:Number(m.avg_improvement_bps),common_concessions:Array.isArray(m.common_concessions)?m.common_concessions.slice(0,8):[]};}
