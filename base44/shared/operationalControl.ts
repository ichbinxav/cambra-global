export type EmergencyState={safe_mode:boolean,communications_paused:boolean,negotiations_paused:boolean,migrations_paused:boolean,billing_issuance_paused:boolean,reason?:string};
export const DEFAULT_EMERGENCY_STATE:EmergencyState=Object.freeze({safe_mode:false,communications_paused:false,negotiations_paused:false,migrations_paused:false,billing_issuance_paused:false});
export async function emergencyState(svc:any):Promise<EmergencyState>{const rows=await svc.entities.EmergencyControl.filter({control_key:'global'},'-updated_at',1).catch(()=>[]);const r=rows?.[0];return r?{safe_mode:r.safe_mode===true,communications_paused:r.communications_paused===true,negotiations_paused:r.negotiations_paused===true,migrations_paused:r.migrations_paused===true,billing_issuance_paused:r.billing_issuance_paused===true,reason:String(r.reason||'')}:DEFAULT_EMERGENCY_STATE}
export async function assertOperationAllowed(svc:any,capability:'communications'|'negotiations'|'migrations'|'billing_issuance'){const s=await emergencyState(svc);const paused=s.safe_mode||capability==='communications'&&s.communications_paused||capability==='negotiations'&&s.negotiations_paused||capability==='migrations'&&s.migrations_paused||capability==='billing_issuance'&&s.billing_issuance_paused;if(paused){const e:any=new Error(`emergency_control_paused:${capability}`);e.code='EMERGENCY_CONTROL_PAUSED';e.state=s;throw e}return s}
