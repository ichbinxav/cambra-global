import{createClientFromRequest}from'npm:@base44/sdk@0.8.41';import{buildFounderSnapshot}from'../../shared/founderOSData.ts';import{callCambraClaude}from'../../shared/commercialModelRouter.ts';
const parse=(t:string)=>{const c=t.replace(/```json\s*/gi,'').replace(/```/g,'').trim();try{return JSON.parse(c)}catch{}const m=c.match(/\{[\s\S]*\}/);if(m)try{return JSON.parse(m[0])}catch{}return null};
Deno.serve(async req=>{try{const b=createClientFromRequest(req),u=await b.auth.me().catch(()=>null);if(!u||u.role!=='admin')return Response.json({ok:false,error:'Forbidden'},{status:403});const s=b.asServiceRole,snap=await buildFounderSnapshot(s),directives=await s.entities.StrategyDirective.filter({status:'active'},'-priority',50).catch(()=>[]);const evidence={generated_at:snap.generated_at,metrics:snap.metrics,attention:snap.attention.slice(0,10),opportunities:snap.opportunities.slice(0,10),risks:snap.risks.slice(0,10),operations:snap.operations,ai_workforce:{task_count_7d:snap.ai_workforce.task_count_7d,failed_7d:snap.ai_workforce.failed_7d},directives:directives.map((x:any)=>({scope:x.scope,directive:x.directive,priority:x.priority}))};const prompt=['You are CAMBRA Chief of Staff. Produce an executive brief using ONLY supplied internal evidence. Never invent a metric, trend, target, forecast or causal claim. If comparison data is absent, do not say a metric increased/decreased. Separate facts from hypotheses. Prioritize founder-only material attention; routine autonomous work should not create cognitive load. Return ONLY JSON:',`{"status":"normal|attention|critical","headline":"","changed_since_last_view":[{"text":"","evidence_refs":[]}],"money":"","growth":"","merchant_value":"","aggregate":"","providers":"","operations":"","ai_workforce":"","founder_actions":[{"priority":0,"title":"","why":"","recommended_action":"","evidence_refs":[]}],"unknowns":[]}`,'EVIDENCE',JSON.stringify(evidence)].join('\n');const d=parse((await callCambraClaude(prompt,{tier:'high_reasoning',maxTokens:2200})).text);if(!d)return Response.json({ok:false,error:'chief_of_staff_unparseable',fallback:evidence},{status:500});return Response.json({ok:true,brief:d,evidence_snapshot:evidence,generated_at:snap.generated_at,truth_boundary:'Narrative is AI-generated; all authoritative values come from evidence_snapshot. Missing evidence remains unknown.'});}catch(e){console.error(e);return Response.json({ok:false,error:'founder_chief_of_staff_failed'},{status:500})}});
