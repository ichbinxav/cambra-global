import{createClientFromRequest}from'npm:@base44/sdk@0.8.41';import{requireAdminOrInternal}from'../../shared/internalGate.ts';
async function incident(s:any,key:string,summary:string,details:any){const old=await s.entities.AutonomyIncident.filter({dedupe_key:key,status:'open'},'-last_seen_at',1).catch(()=>[]),row={dedupe_key:key,domain:'provider_revenue',severity:'warning',status:'open',workflow_state:'investigating',owner_type:'finance',automation_eligibility:'bounded_auto',summary,details_json:details,financial_impact_minor:Math.abs(Number(details.delta_minor||0)),customer_impact:'none',legal_risk:'low',first_seen_at:old[0]?.first_seen_at||new Date().toISOString(),last_seen_at:new Date().toISOString()};if(old[0])await s.entities.AutonomyIncident.update(old[0].id,row);else await s.entities.AutonomyIncident.create(row)}
Deno.serve(async req=>{try{const b=createClientFromRequest(req),body=await req.json().catch(()=>({})),g=await requireAdminOrInternal(req,b,body);if(!g.ok)return g.response;const s=b.asServiceRole,stmts=await s.entities.ProviderRevenueStatement.filter({status:{$in:['received','parsed','mismatch']}},'-received_at',1000).catch(()=>[]);let reconciled=0,mismatches=0;for(const st of stmts){const rows=await s.entities.ProviderRevenueLedger.filter({provider_id:st.provider_id,period:st.period},'-updated_at',5000).catch(()=>[]),expected=rows.reduce((a:number,x:any)=>a+Number(x.accrued_amount_minor||x.expected_amount_minor||0),0),reported=Number(st.reported_amount_minor||0),delta=reported-expected,tolerance=Math.max(100,Math.round(expected*.005));if(Math.abs(delta)<=tolerance){await s.entities.ProviderRevenueStatement.update(st.id,{status:'reconciled',reconciled_at:new Date().toISOString()});for(const r of rows.filter((x:any)=>['accrued','validation_pending'].includes(x.state)))await s.entities.ProviderRevenueLedger.update(r.id,{state:'validation_pending',provider_statement_id:st.id,updated_at:new Date().toISOString()});reconciled++;}else{await s.entities.ProviderRevenueStatement.update(st.id,{status:'mismatch',reconciled_at:new Date().toISOString()});await incident(s,`provider-revenue-mismatch:${st.id}`,'Provider revenue statement does not reconcile to CAMBRA attribution/accrual',{provider_id:st.provider_id,period:st.period,expected_minor:expected,reported_minor:reported,delta_minor:delta,statement_id:st.id});await s.entities.Event.create({brand_id:'_platform',event_type:'PROVIDER_REVENUE_MISMATCH',source:'provider_revenue_reconciliation',entity_type:'ProviderRevenueStatement',entity_id:st.id,payload_json:{provider_id:st.provider_id,period:st.period,expected_minor:expected,reported_minor:reported,delta_minor:delta},status:'processed',processed_at:new Date().toISOString()}).catch(()=>null);mismatches++;}}return Response.json({ok:true,reconciled,mismatches})}catch(e){console.error(e);return Response.json({ok:false,error:'provider_revenue_reconciliation_failed'},{status:500})}});
