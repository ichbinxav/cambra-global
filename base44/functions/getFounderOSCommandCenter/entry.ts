import{createClientFromRequest}from'npm:@base44/sdk@0.8.41';import{buildFounderSnapshot}from'../../shared/founderOSData.ts';import{FOUNDER_METRICS}from'../../shared/founderOSCore.ts';
Deno.serve(async req=>{try{const b=createClientFromRequest(req),u=await b.auth.me().catch(()=>null);if(!u||u.role!=='admin')return Response.json({ok:false,error:'Forbidden'},{status:403});const d=await buildFounderSnapshot(b.asServiceRole);return Response.json({...d,metric_catalog:FOUNDER_METRICS,principle:'INSIGHT → RECOMMENDATION → ACTION; money remains deterministic and AI explanations never rewrite authoritative state.'})}catch(e){console.error(e);return Response.json({ok:false,error:'founder_os_command_center_failed'},{status:500})}});
