import{useEffect,useState}from'react';import{base44}from'@/api/base44Client';import{Landmark,RefreshCw}from'lucide-react';const eur=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(Number(n||0));export default function AdminFinance(){const[d,setD]=useState(null);const load=async()=>{const r=await base44.functions.invoke('getFinancialControlTower',{});setD(r?.data||r)};useEffect(()=>{load()},[]);if(!d)return <div className="p-8 text-sm text-muted-foreground">Loading Financial Control Tower…</div>;const m=d.metrics||{};return <div className="space-y-6"><div className="flex justify-between"><div><div className="flex items-center gap-2"><Landmark size={20}/><h1 className="text-xl font-black">Financial Control Tower</h1></div><p className="text-sm text-muted-foreground mt-2">Estimated, verified, billable, invoiced and collected values are deliberately separated.</p></div><button onClick={load} className="text-xs inline-flex gap-2 items-center"><RefreshCw size={12}/>Refresh</button></div><div className="grid grid-cols-2 lg:grid-cols-5 gap-3">{[['Estimated savings',eur(m.estimated_savings)],['Verified savings',eur(m.verified_savings)],['Billable savings',eur(m.billable_savings)],['Invoiced revenue',eur(m.invoiced_revenue)],['Collected cash',eur(m.collected_cash)],['Outstanding',eur(m.outstanding_cash)],['Disputed',eur(m.disputed_cash)],['Open invoices',m.open_invoices||0],['Pending approvals',m.pending_approvals||0],['Open incidents',m.open_incidents||0]].map(([k,v])=><div key={k} className="rounded-xl border border-border/60 p-4"><div className="text-[10px] uppercase text-muted-foreground">{k}</div><div className="text-xl font-black mt-1">{v}</div></div>)}</div><div className="rounded-2xl border border-border/60 p-5"><h2 className="font-bold mb-3">Revenue state machine</h2><div className="grid grid-cols-2 md:grid-cols-4 gap-3">{Object.entries(d.revenue_states||{}).map(([k,v])=><div key={k} className="rounded-lg bg-secondary/40 p-3"><div className="text-[10px] uppercase text-muted-foreground">{k.replaceAll('_',' ')}</div><div className="font-black text-lg">{v}</div></div>)}</div></div><div className="rounded-2xl border border-border/60 p-5 text-sm"><b>Financial truth boundary</b><div className="grid md:grid-cols-5 gap-3 mt-3 text-muted-foreground">{Object.entries(d.truth_boundary||{}).map(([k,v])=><div key={k}><span className="text-foreground font-semibold">{k}</span><br/>{v}</div>)}</div></div></div>}