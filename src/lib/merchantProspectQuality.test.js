import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const read=p=>fs.readFileSync(path.join(ROOT,p),'utf8');describe('merchant prospect quality',()=>{it('scores economic and payments fit without inventing GMV or fees',()=>{const s=read('base44/functions/leadScoringAgent/entry.ts');for(const k of ['Commerce/payment fit','Economic potential','Payments complexity','Decision-maker quality','Timing/growth signal','Data/contact confidence','No inventar GMV','No afirmar fees sin evidencia'])expect(s).toContain(k)});it('filters obvious non-merchants before spending downstream enrichment capacity',()=>{const s=read('base44/functions/leadDiscoveryAgent/entry.ts');for(const k of ['merchantDiscoveryCandidate','NON_MERCHANT_ORG','university','agency','logistics','organization_generic','rejected_count'])expect(s).toContain(k)});it('volume send requires corporate-domain contact and minimum commerce/economic evidence',()=>{const s=read('base44/functions/outboundVolumeWorker/entry.ts');expect(s).toContain('corporateDomain');expect(s).toContain('emailDomain');expect(s).toContain('commerce_fit');expect(s).toContain('economic_potential')})});