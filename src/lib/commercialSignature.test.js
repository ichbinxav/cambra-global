import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const R=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const s=fs.readFileSync(path.join(R,'base44/functions/commercialSendMessage/entry.ts'),'utf8');describe('commercial email signatures',()=>{it('enforces a deterministic signature after quality validation',()=>{expect(s).toContain('function ensureSignature');expect(s).toContain('const signedText=ensureSignature');expect(s).toContain("Xavi Martínez\\nFounder, CAMBRA");expect(s).toContain("CAMBRA\\nInfrastructure Intelligence");expect(s).toContain('text_body:signedText')});it('sends signed body through both Outlook and Resend',()=>{expect(s).toContain("content:signedText");expect(s).toContain('text:signedText')})});