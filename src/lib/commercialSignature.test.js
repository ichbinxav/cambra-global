import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const R=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const s=fs.readFileSync(path.join(R,'base44/functions/commercialSendMessage/entry.ts'),'utf8');describe('commercial email signatures',()=>{it('enforces a deterministic signature after quality validation',()=>{expect(s).toContain('function ensureSignature');expect(s).toContain('const signedText=ensureSignature');expect(s).toContain("name:'Xavi M. Contero',title:'Founder, CAMBRA',email:'xavi@cambra.global'");expect(s).toContain("CAMBRA Payments");expect(s).toContain('payments@contact.cambra.global');expect(s).toContain('xavi@cambra.global');expect(s).toContain('www.cambra.global');expect(s).toContain('text_body:signedText')});it('sends signed body through both Outlook and Resend',()=>{expect(s).toContain("contentType:'HTML'");expect(s).toContain('content:signedHTML');expect(s).toContain('text:signedText');expect(s).toContain('html:signedHTML');expect(s).toContain('CAMBRA_LOGO')})});