import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const R=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const s=fs.readFileSync(path.join(R,'base44/functions/commercialSendMessage/entry.ts'),'utf8');describe('commercial email signatures',()=>{it('enforces an organizational signature derived from the configured sending identity',()=>{expect(s).toContain('function ensureSignature');expect(s).toContain('const signedText=ensureSignature');expect(s).toContain("title:'Founder Office',email");expect(s).toContain('configuredSignatureEmail');expect(s).toContain("Deno.env.get('CAMBRA_OUTLOOK_SIGNATURE_EMAIL')");expect(s).not.toContain("name:'Xavi M. Contero'");expect(s).not.toContain('xavi@cambra.global');expect(s).toContain('www.cambra.global');expect(s).toContain('text_body:signedText')});it('omits unknown mailbox identity and fails closed for unconfigured Resend',()=>{expect(s).toContain("i.email?`Mail: ${i.email}`:null");expect(s).toContain("new Error('resend_from_identity_required')")});it('sends signed body through Outlook, Resend and the Instantly transport adapter',()=>{expect(s).toContain("contentType:'HTML'");expect(s).toContain('content:signedHTML');expect(s).toContain('text:signedText');expect(s).toContain('html:signedHTML');expect(s).toContain("provider==='instantly'");expect(s).toContain('CAMBRA_LOGO')})});
