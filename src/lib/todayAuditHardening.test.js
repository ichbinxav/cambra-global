import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const R=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const r=p=>fs.readFileSync(path.join(R,p),'utf8');describe('today audit hardening',()=>{it('Resend inbound persists same deterministic reply timing gates as Outlook',()=>{const s=r('base44/functions/resendInboundWebhook/entry.ts');expect(s).toContain('computeInboundReplySchedule');expect(s).toContain('earliest_reply_at:timing.earliest_reply_at');expect(s).toContain('scheduled_send_at:timing.scheduled_send_at');expect(s).toContain('next_action_at:timing.scheduled_send_at')});it('merchant volume discovery covers both configured countries and prevents durable duplicate contact/company sends',()=>{const d=r('base44/functions/alwaysOnLeadDiscoveryWorker/entry.ts');const s=r('base44/functions/outboundVolumeWorker/entry.ts');expect(d).toContain("==='FR'?'France'");expect(d).toContain("==='ES'?'Spain'");expect(d).toContain('seen');expect(s).toContain('contactedDomains');expect(s).toContain("counterparty_email:email");expect(s).toContain('relatedDomain')});it('learning worker is scheduled and old premium merchant scheduler disabled',()=>{expect(r('base44/functions/acquisitionLearningWorker/function.jsonc')).toContain('"is_active":true');expect(r('base44/functions/autonomousCommercialWorker/function.jsonc')).toContain('"is_active": false')})});