import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const R=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const r=p=>fs.readFileSync(path.join(R,p),'utf8');describe('merchant opportunity v2',()=>{it('uses deterministic majority scoring with provenance/confidence',()=>{const s=r('base44/shared/leadScoringResilience.ts');expect(s).toContain('det.opportunity_score*0.7+(llm as number)*0.3');expect(s).toContain("scoring_version:'merchant-opportunity-v2'");expect(s).toContain('evidence_confidence')});it('prioritizes opportunity x confidence and dedupes company before send',()=>{const s=r('base44/functions/outboundVolumeWorker/entry.ts');expect(s).toContain('learnedPriority');expect(s).toContain('b.priority-a.priority');expect(s).toContain('seenCompanies');expect(s).toContain('<0.55')});it('deterministic model includes size commerce payments and timing evidence',()=>{const s=r('base44/shared/merchantOpportunity.ts');for(const k of ['employees','revenue','monthly_traffic','store_count','payment_provider','commerce_platform','evidence_count'])expect(s).toContain(k)})});
