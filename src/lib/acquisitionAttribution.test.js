import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const R=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..','..');const r=p=>fs.readFileSync(path.join(R,p),'utf8');describe('acquisition economic provenance',()=>{it('attributes only exact unique contact email and unique sent thread',()=>{const s=r('base44/functions/acquisitionAttributionWorker/entry.ts');expect(s).toContain('contact_email:email');expect(s).toContain('eligible.length!==1');expect(s).toContain('sent.length!==1');expect(s).toContain("attribution_method:'exact_contact_email'");expect(s).not.toContain('fuzzy')});it('verified savings learning requires deterministic attribution',()=>{const s=r('base44/functions/acquisitionLearningWorker/entry.ts');expect(s).toContain('AcquisitionAttribution');expect(s).toContain("measurement_mode:'fully_verified'");expect(s).toContain('verifiedByBrand');expect(s).toContain("outcomeValue('verified_savings',vs)")});it('learning cannot write financial records',()=>{const s=r('base44/functions/acquisitionLearningWorker/entry.ts');expect(s).not.toContain('MonthlySavingsReport.update');expect(s).not.toContain('Invoice.update');expect(s).not.toContain('BillingRule.update')})});